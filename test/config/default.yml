imports:
    - { resource: "routing/default.yml", property: "http_router.routes.default" }
    - { resource: "routing/default.yml", property: "routing.a.b.c" }
    - { resource: "security.yml", property: "security" }

foo: "bar"
list:
    - 1
    - 2
    - 3

parameters:
    a: "b"
    c: "d"

routing:
    debug: true
